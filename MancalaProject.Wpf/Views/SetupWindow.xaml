<Window x:Class="MancalaProject.Wpf.Views.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mancala — Setup"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5EFE0"
        FontFamily="Segoe UI">

    <Grid Margin="35,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Mancala"
                   FontSize="36"
                   FontWeight="Bold"
                   Foreground="#3E2A1F"
                   Margin="0,0,0,24"/>

        <!-- Difficulty -->
        <GroupBox Grid.Row="1"
                  Header="Difficulty"
                  Foreground="#3E2A1F"
                  FontSize="15"
                  FontWeight="SemiBold"
                  Padding="8"
                  Margin="0,0,0,18">
            <StackPanel Margin="6,8">
                <RadioButton Content="Easy"
                             GroupName="Difficulty"
                             IsChecked="{Binding IsEasy}"
                             FontSize="16"
                             FontWeight="Normal"
                             Margin="0,5"/>
                <RadioButton Content="Medium"
                             GroupName="Difficulty"
                             IsChecked="{Binding IsMedium}"
                             FontSize="16"
                             FontWeight="Normal"
                             Margin="0,5"/>
                <RadioButton Content="Hard"
                             GroupName="Difficulty"
                             IsChecked="{Binding IsHard}"
                             FontSize="16"
                             FontWeight="Normal"
                             Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <!-- Who plays first -->
        <GroupBox Grid.Row="2"
                  Header="Who plays first"
                  Foreground="#3E2A1F"
                  FontSize="15"
                  FontWeight="SemiBold"
                  Padding="8">
            <StackPanel Margin="6,8">
                <RadioButton Content="I play first"
                             GroupName="Starter"
                             IsChecked="{Binding PlayerStarts}"
                             FontSize="16"
                             FontWeight="Normal"
                             Margin="0,5"/>
                <RadioButton Content="Computer plays first"
                             GroupName="Starter"
                             IsChecked="{Binding ComputerStarts}"
                             FontSize="16"
                             FontWeight="Normal"
                             Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <!-- Buttons row -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button Content="Cancel"
                    Width="100"
                    Height="38"
                    FontSize="14"
                    Margin="0,0,12,0"
                    IsCancel="True"/>
            <Button Content="Start"
                    Width="100"
                    Height="38"
                    FontSize="14"
                    FontWeight="SemiBold"
                    IsDefault="True"
                    Click="OnStartClick"/>
        </StackPanel>
    </Grid>
</Window>
